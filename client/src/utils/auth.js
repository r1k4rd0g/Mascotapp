export const isAuthenticated = () => {
    //const token = localStorage.getItem('token');
    //return !! token; //devuelve true si existe token, utiliza doble negación para convertir el valor a booleano
    return true;
}
